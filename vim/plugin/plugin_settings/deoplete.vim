" FUNCTIONS {{{
" }}}

" SETTINGS {{{
	let g:deoplete#enable_at_startup = 1
  let g:deoplete#omni_patterns = {}
  let g:deoplete#omni_patterns.reason = '.*'
  let g:deoplete#omni_patterns.ocaml = '.*'

  let g:deoplete#ignore_sources = {}
  let g:deoplete#ignore_sources.ocaml = ['buffer', 'around']
  let g:deoplete#ignore_sources.reason = ['buffer', 'around']
" }}}

" MAPPINGS {{{
" }}}
